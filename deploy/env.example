# Copy to /opt/snubworks-git/.env and set to the network TeamForce's Caddy is on.
# Find it (step 1 of DEPLOY.md):
#   docker inspect $(docker ps --filter name=caddy -q) \
#     -f '{{range $k,$v := .NetworkSettings.Networks}}{{$k}} {{end}}'
CADDY_NETWORK=teamforce-git_proxy
